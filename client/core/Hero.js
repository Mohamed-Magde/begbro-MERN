import React from "react";
import img from "../assets/img/hero.png";

const Hero = () => {
  return (
    <header class="hero">
      <img src={img} alt />
    </header>
  );
};

export default Hero;
